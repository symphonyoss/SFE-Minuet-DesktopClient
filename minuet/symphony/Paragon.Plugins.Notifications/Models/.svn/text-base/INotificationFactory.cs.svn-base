using Paragon.Plugins.Notifications.Contracts;

namespace Paragon.Plugins.Notifications.Models
{
    public interface INotificationFactory
    {
        Notification Create(NotificationOptions options);
        Notification Create(string notificationId, NotificationOptions options);
    }
}