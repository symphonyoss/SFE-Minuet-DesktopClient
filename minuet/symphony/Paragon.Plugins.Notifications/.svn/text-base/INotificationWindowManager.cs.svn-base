using Paragon.Plugins.Notifications.ViewModels;

namespace Paragon.Plugins.Notifications
{
    public interface INotificationWindowManager
    {
        NotificationWindowViewModel ShowNotificationWindow();
        UserConfigurationWindowViewModel ShowSettings(IApplicationWindow owner);
        void Shutdown();
    }
}